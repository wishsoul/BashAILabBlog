---
import { getCollection, render } from "astro:content";
import EditorialPage from "../../../components/content/EditorialPage.astro";
import { withBase } from "../../../lib/urls";

const entry = (await getCollection("pages")).find(
  (page) => page.id === "zh/about" && !page.data.draft,
);

if (!entry) return Astro.redirect(withBase("/about/"));

const { Content } = await render(entry);
---

<EditorialPage entry={entry} translationPath="/about/">
  <Content />
</EditorialPage>
